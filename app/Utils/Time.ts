/**
 * Time enums to use in the system
 *
 * @category Utils
 * @module Time
 *
 * @enum Days
 */
export enum Days {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday,
}

/**
 * @enum OpeningHourTypes
 */
export enum OpeningHourTypes {
    Default,
    Exception,
}
