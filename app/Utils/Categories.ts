/**
 * @category Utils
 * @module Categories
 *
 * @enum Categories The enum type for the different categories available
 * Why the numbering is done in this way is in order to differentiate between
 * the IDs that are applied upon setting up the server during migration and
 * from the constant valuse for each category to always use in backend and
 * frontend.
 */
export enum Categories {
    Breakfast = 10,
    Brunch = 20,
    Lunch = 30,
    Fika = 40,
    Sport = 50,
    Restaurant = 60,
    Pub = 70,
    Culture = 80,
    Gasque = 90,
    Club = 100,
    Consert = 110,
    Other = 120,
}
